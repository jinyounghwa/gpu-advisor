# GPU 구매 타이밍 예측 AI 시스템
## 크롤러부터 AlphaZero AI까지 - 완전 가이드

**작성일:** 2026-02-14
**최종 업데이트:** 2026-03-15
**프로젝트:** GPU Purchase Timing Advisor
**방식:** AlphaZero/MuZero (바둑 승률 방식)

> 본 문서는 **2026-03-15 기준 종합 스냅샷**입니다.
> 현재 운영 기준(실행 명령, 리포트 경로, 실데이터 축적 상태)은 `README.md`와 `CRAWLER_GUIDE.md`를 우선 참조하세요.

---

### 0. 2026-03-15 운영 현황

- **데이터 수집 상태**: 일일 수집 정상 동작 중 (LaunchAgent via `~/Library/LaunchAgents/com.gpu-advisor.daily-crawl.plist`)
- **데이터 윈도우**: 30일 달성 임박 / 자동 학습 트리거 조건 확인 중
- **자동화 파이프라인**: `run_daily.py` → 수집 → Feature Engineering → 자동 학습/릴리즈 오케스트레이션
- **최신 리포트 위치**:
  - `docs/reports/latest_data_status.json` — 일일 데이터 상태
  - `docs/reports/latest_auto_training_status.json` — 자동 학습 결정 결과
  - `docs/reports/latest_release_report.json` — 릴리즈 판정 결과

#### 0.1 현재 아키텍처 상태 요약

| 컴포넌트 | 상태 | 비고 |
|---------|------|------|
| RepresentationNetwork h | ✅ 완성 | 256D 입력, 잔차 연결 FeedForward×3, GELU |
| DynamicsNetwork g | ✅ 완성 | Gaussian reward (μ, log σ²), 잔차 블록×4 |
| PredictionNetwork f | ✅ 완성 | Policy + Value, 잔차 블록×4 |
| ActionModel a | ✅ 완성 | 학습 임베딩(16D) + 맥락 기반 행동 Prior (~43K params) |
| MCTSEngine | ✅ 완성 | PUCT 공식, Dirichlet noise, 5-step rollout |
| Fine-tuner | ✅ 완성 | 5개 손실 함수, AdamW, 그래디언트 클리핑 |
| 자동 학습 오케스트레이션 | ✅ 완성 | 30일 도달 후 자동 학습, 7일 주기 재학습 |
| 릴리즈 파이프라인 | ✅ 완성 | 7개 품질 게이트, 자동 리포트 생성 |
| LaunchAgent (cron 대체) | ✅ 완성 | macOS TCC 문제 해결, 로그: `~/Library/Logs/gpu-advisor/cron.log` |

---

## 📋 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [시스템 아키텍처](#2-시스템-아키텍처)
3. [데이터 수집 시스템 (크롤러)](#3-데이터-수집-시스템-크롤러)
4. [Feature Engineering (256차원)](#4-feature-engineering-256차원)
5. [AlphaZero AI 엔진](#5-alphazero-ai-엔진)
6. [학습 프로세스](#6-학습-프로세스)
7. [자동 학습 오케스트레이션](#7-자동-학습-오케스트레이션)
8. [실전 사용법](#8-실전-사용법)
9. [전체 데이터 흐름](#9-전체-데이터-흐름)
10. [로드맵 및 일정](#10-로드맵-및-일정)
11. [FAQ](#11-faq)

---

## 1. 프로젝트 개요

### 1.1 목적

**"부품을 지금 사야 할까? 나중에 사야 할까?"**

GPU(그래픽카드)를 구매할 때 최적의 타이밍을 예측하는 AI 시스템입니다.
바둑에서 AlphaGo가 승률을 계산하듯이, 이 시스템은 **구매 적정도(0~100점)**를 계산합니다.

### 1.2 핵심 개념

```
입력:  GPU 모델명 (예: RTX 5060)
처리:  MCTS 50회 시뮬레이션 → 4개 신호 혼합 정책 보정
출력:  BUY_NOW / WAIT_SHORT / WAIT_LONG / HOLD / SKIP + 신뢰도
```

### 1.3 바둑 승률과의 비교

| 바둑 (AlphaGo) | GPU 구매 AI |
|----------------|-------------|
| 현재 국면 분석 | 현재 가격 + 시장 분석 |
| 여러 수 시뮬레이션 | 여러 구매 시점 시뮬레이션 |
| 승률 70% | 구매 적정도 70% |
| "여기에 두세요" | "지금 사세요 / N주 대기" |

### 1.4 왜 AlphaZero인가?

단순 가격 예측이 아닌 **전략적 판단**이 필요하기 때문입니다.

- ❌ 단순 회귀: "다음 주 가격은 60만원"
- ✅ AlphaZero: "1주일 대기하면 +5% 이득, 지금 사면 +2% 이득 → 대기 추천"

---

## 2. 시스템 아키텍처

### 2.1 전체 구조

```
┌─────────────────────────────────────────────────────────────────────┐
│                         GPU 구매 타이밍 AI                            │
└─────────────────────────────────────────────────────────────────────┘

[1] 데이터 수집 (LaunchAgent 00:00 자동)
    ↓
    • 다나와 GPU 가격 (24개 모델 목표)
    • 환율 (USD/KRW, JPY/KRW, EUR/KRW)
    • 뉴스 + 키워드 기반 감성 분석
    ↓
[2] Feature Engineering
    ↓
    • 256차원 상태 벡터 생성 (가격/환율/뉴스/시장/시간/기술지표)
    ↓
[3] AlphaZero AI (World Model + MCTS)
    ↓
    ├─ h: Representation Network  → 256D Latent State
    ├─ g: Dynamics Network        → 다음 상태 + Gaussian reward
    ├─ f: Prediction Network      → Policy logits + Value
    ├─ a: Action Model            → 맥락 기반 행동 사전 확률
    └─ MCTS (PUCT, Dirichlet noise, 5-step rollout)
    ↓
[4] 정책 보정 (4-signal blend)
    ↓
    45% MCTS + 25% Reward + 15% f-net prior + 15% ActionModel prior
    ↓
[5] 안전 게이트 → 최종 결정
    ↓
    http://localhost:8000
```

### 2.2 기술 스택

**백엔드:**
- Python 3.13
- PyTorch 2.x (Apple MPS 가속)
- FastAPI (REST API)
- NumPy, Pandas

**프론트엔드:**
- Next.js 15
- React 19
- TypeScript

**AI 엔진:**
- AlphaZero/MuZero 스타일 h/g/f + ActionModel a
- MCTS (PUCT 공식, Dirichlet noise ε=0.25 α=0.03)
- AdamW 기반 Fine-tuning (500 steps, lr=1e-4)

**자동화:**
- macOS LaunchAgent (cron 대체, TCC 문제 해결 완료)
- 자동 30일 학습 + 7일 주기 재학습 오케스트레이션

---

## 3. 데이터 수집 시스템 (크롤러)

### 3.1 개요

매일 자정 LaunchAgent로 자동 실행됩니다.

```
~/Library/LaunchAgents/com.gpu-advisor.daily-crawl.plist
→ 로그: ~/Library/Logs/gpu-advisor/cron.log
→ 상세 로그: logs/daily_crawl.log
```

### 3.2 크롤러 구조

```
crawlers/
├── danawa_crawler.py          # 다나와 GPU 가격
├── exchange_rate_crawler.py   # 환율 (open.er-api.com)
├── news_crawler.py            # 뉴스 + 감성 분석 (Google News RSS)
├── feature_engineer.py        # 256차원 Feature 생성
├── auto_training.py           # 자동 학습/릴리즈 오케스트레이션
└── run_daily.py               # 일일 실행 스크립트 (락 기반 중복 방지)
```

### 3.3 수집 데이터

#### (1) 다나와 GPU 가격

**수집 대상:** 24개 GPU 모델 (당일 수집 성공 수는 변동)

```json
{
  "date": "2026-03-15",
  "products": [
    {
      "chipset": "RTX 5060",
      "lowest_price": 610986,
      "seller_count": 15,
      "stock_status": "in_stock"
    }
  ]
}
```

**수집 모델:**
- NVIDIA 5000: RTX 5090, 5080, 5070 Ti, 5070, 5060 Ti, 5060, 5050
- NVIDIA 4000: RTX 4090, 4080, 4070 Ti, 4070, 4060 Ti, 4060
- AMD: RX 9070 XT, 9060 XT, 7900 XTX, 7900 XT, 7800 XT, 7700 XT, 7600, 6600
- Intel: Arc B580, B570, A770

#### (2) 환율

```json
{
  "date": "2026-03-15",
  "rates": {
    "USD/KRW": 1453.05,
    "JPY/KRW": 942.27,
    "EUR/KRW": 1566.62
  }
}
```

#### (3) GPU 뉴스 + 감성 분석

키워드 기반 감성 점수 계산:
```python
score = (pos_keywords - neg_keywords) / (pos_keywords + neg_keywords)
# -1.0 ~ +1.0, 중립 기준 ±0.1
```

긍정 키워드: `price drop, cheaper, discount, sale, in stock, launch, ...`
부정 키워드: `price rise, shortage, tariff, sold out, scalper, ...`

### 3.4 자동화 (LaunchAgent)

```bash
# 상태 확인
launchctl print gui/$(id -u)/com.gpu-advisor.daily-crawl

# 수동 실행
python3 crawlers/run_daily.py

# 릴리즈 생략(수집+Feature만)
python3 crawlers/run_daily.py --skip-release

# 로그 확인
tail -f ~/Library/Logs/gpu-advisor/cron.log
tail -f logs/daily_crawl.log
```

---

## 4. Feature Engineering (256차원)

### 4.1 개요

원시 데이터(가격, 환율, 뉴스)에서 **256차원 상태 벡터**를 생성합니다.

```
┌──────────────────────────────────────────────────────────────────┐
│                     256차원 Feature 구조                           │
├──────────────────────────────────────────────────────────────────┤
│  [  0 -  59]  가격 Feature (60차원)                                │
│               • 현재가, 전일가, 전주가, 전월가 (정규화)              │
│               • 이동평균 (MA7, MA14, MA30)                         │
│               • 가격 변화율 (1일, 7일)                              │
│               • 변동성 (표준편차), 최고가/최저가                     │
│               • 추세 지표                                           │
│                                                                  │
│  [ 60 -  79]  환율 Feature (20차원)                               │
│               • USD/KRW, JPY/KRW, EUR/KRW 정규화                  │
│               • 환율 변화율 (1일, 7일), 이동평균                    │
│                                                                  │
│  [ 80 - 109]  뉴스 Feature (30차원)                              │
│               • 감성 점수 (1일, 7일 평균)                          │
│               • 긍정/부정/중립 기사 수                              │
│               • 키워드 빈도                                        │
│                                                                  │
│  [110 - 129]  시장 Feature (20차원)                              │
│               • 판매자 수 정규화                                   │
│               • 재고 상태 인코딩 (in_stock=1.0, low=0.5, out=0.0) │
│                                                                  │
│  [130 - 149]  시간 Feature (20차원)                              │
│               • 요일 (0.0~1.0), 월 (0.0~1.0)                     │
│               • 연말 플래그, 계절성                                │
│                                                                  │
│  [150 - 255]  기술 지표 (106차원)                                │
│               • RSI, MACD, 볼린저 밴드, 모멘텀                    │
│               • ※ 26일+ 이력 필요 지표(MACD)는 누적 전 0.0 유지  │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

### 4.2 정규화

모든 Feature는 [0, 1] 또는 [-1, 1] 범위로 정규화:
```python
normalized_price = current_price / 10_000_000
normalized_rsi = rsi / 100
normalized_sentiment = (sentiment + 1) / 2  # -1~1 → 0~1
```

---

## 5. AlphaZero AI 엔진

### 5.1 아키텍처 개요

```
[입력] GPU 모델명 "RTX 5060"
  ↓ resolve + state vector 로드
[256D 상태 벡터]
  ↓
[h] Representation Network
  • Linear(256, 256) + LayerNorm
  • FeedForward×3 (잔차 연결: x = x + ff(x))
  • 각 블록: 256→512(GELU)→256
  ↓ 256D Latent State
[f] Prediction Network → Policy logits(5D) + Value(1D)
  ↓
[MCTS] 50회 시뮬레이션
  • PUCT: Q + c·P·√N_parent / (1 + N_child), c=√2
  • 루트 Dirichlet noise: ε=0.25, α=0.03
  • 5-step rollout: g 호출
  ↓
[g] Dynamics Network (rollout 중 호출)
  • 입력: [latent(256D); action(5D)]
  • 4개 잔차 블록 (256→512(GELU)→256)
  • 출력: next_latent(256D), reward_μ, reward_log σ²
  ↓
[a] Action Model (policy calibration)
  • ActionPriorNetwork: 256→128→64→5
  ↓
[정책 보정]
  • 45%·MCTS + 25%·Reward + 15%·f-net prior + 15%·ActionModel prior
  • Anti-collapse: entropy < 0.65 시 uniform(1/5) 혼합
  ↓
[안전 게이트]
  • confidence < 0.25 → HOLD
  • entropy > 1.58 → HOLD
  ↓
[최종 결정] BUY_NOW / WAIT_SHORT / WAIT_LONG / HOLD / SKIP
```

### 5.2 네트워크 상세

#### (1) Representation Network h(s)

```python
class RepresentationNetwork(nn.Module):
    # 입력: 256D GPU 시장 상태 벡터 (feature_engineer 출력)
    # 출력: 256D Latent State

    def forward(self, state_tensor):
        x = self.input_embedding(x)   # Linear(256, 256) + LayerNorm
        # 잔차 연결: 그래디언트 소실 방지
        x = x + self.ff1(x)
        x = x + self.ff2(x)
        x = x + self.ff3(x)
        x = self.layer_norm2(x)
        return self.output_layer(x)   # (batch, 256)

    # ※ PositionalEncoding: seq_len=1에서 의미 없으므로 forward에서 적용 안 함
    #   (체크포인트 호환성을 위해 __init__에만 유지)
```

#### (2) Dynamics Network g(s, a)

```python
class DynamicsNetwork(nn.Module):
    # 입력: latent(256D) + action one-hot(5D)
    # 출력: next_latent(256D), reward_mean, reward_logvar(=log σ²)

    def forward(self, s_t, a_t):
        x = torch.cat([s_t, a_t], dim=-1)  # (batch, 261)
        x = self.input_layer(x)
        x = self.layer_norm1(x)
        for block in self.blocks:
            x = x + block(x)               # 잔차 연결
        x = self.layer_norm2(x)
        s_tp1 = self.next_state_head(x)
        reward_mean = self.reward_mean_head(x).squeeze(-1)
        reward_logvar = self.reward_logvar_head(x).squeeze(-1)  # raw, 활성화 없음
        return s_tp1, reward_mean, reward_logvar

    # σ² = exp(reward_logvar)  — Gaussian NLL로 불확실성 헤드 학습
```

#### (3) Prediction Network f(s)

```python
class PredictionNetwork(nn.Module):
    # 입력: latent(256D)
    # 출력: policy_logits(5D), value(1D, Tanh 범위 [-1, 1])

    def forward(self, latent):
        x = self.input_layer(latent)
        for block in self.blocks:
            x = x + block(x)           # 잔차 연결
        x = self.layer_norm2(x)
        policy_logits = self.policy_head(x)   # 512→2048→5
        value = torch.tanh(self.value_head(x)) # 512→512→1
        return policy_logits, value
```

#### (4) Action Model a(z)

```python
class ActionModel(nn.Module):
    # 두 가지 역할:
    # 1. ActionEmbeddingLayer: action index(int) → 16D embedding
    # 2. ActionPriorNetwork: latent(256D) → action prior probs(5D)
    #
    # gpu_purchase_agent.py 의 하드코딩 휴리스틱(utility_bias)을 대체

    def get_prior(self, latent_state):
        # latent_state: (batch, 256) or (256,) — 둘 다 지원
        probs = self.prior_net.prior_probs(latent_state)
        return probs  # 합이 1인 확률 분포
```

### 5.3 MCTS (AlphaZero 스타일)

```
PUCT 선택 기준:
  UCB(s,a) = Q(s,a) + c · P(s,a) · √N(s) / (1 + N(s,a))
  c = √2 ≈ 1.4142

루트 노드 탐색 다양성 (Dirichlet noise):
  P'(s,a) = (1 - ε) · P(s,a) + ε · Dir(α)
  ε=0.25, α=0.03

롤아웃 (5 steps):
  각 스텝: f(z)로 행동 샘플링 → g(z, a)로 다음 상태·보상 예측
  누적 보상: Σ γ^k · r_k + γ^5 · V(z_5), γ=0.99
```

### 5.4 체크포인트 구조

```python
checkpoint = {
    "h_state_dict": ...,   # Representation Network 가중치
    "g_state_dict": ...,   # Dynamics Network 가중치
    "f_state_dict": ...,   # Prediction Network 가중치
    "a_state_dict": ...,   # Action Model 가중치 (없으면 하드코딩 휴리스틱 폴백)
    "meta": {
        "action_labels": {0:"BUY_NOW", 1:"WAIT_SHORT", ...},
        "num_samples": 620,
        "action_prior": [0.15, 0.25, 0.35, 0.20, 0.05],  # 경험적 행동 분포
        "train_config": {"num_steps": 500, "batch_size": 32, "lr": 1e-4},
        "schema_version": "agent-v1",
    }
}
```

---

## 6. 학습 프로세스

### 6.1 Transition 데이터셋 구성

학습은 연속된 날짜 쌍에서 전이 샘플을 생성합니다:

```python
# (state_t, state_{t+1}, action, reward, value_target)
pct_change = (price_{t+1} - price_t) / price_t

# 행동 레이블링 (평가기 보상 함수와 일치):
if pct_change >= +0.02:  action = BUY_NOW    # 내일 비싸지므로 오늘 구매
elif pct_change <= -0.02: action = WAIT_LONG  # 크게 하락 예정 → 기다려라
elif pct_change <= -0.005: action = WAIT_SHORT
else:                     action = HOLD

reward = clip(pct_change, -1.0, 1.0)
value_target = tanh(pct_change * 8.0)
```

> **설계 원칙**: SKIP(4) 레이블은 가격 변화율로 정할 수 없음
> (가격 하락 시 WAIT가 최적이며, SKIP의 평가 보상은 항상 음수)

### 6.2 Loss 함수 (5개 + 정규화)

```python
total_loss = (
    policy_loss              # 교차 엔트로피 (클래스 가중치 적용)
    + value_loss             # MSE (value 예측)
    + dynamics_loss          # MSE (next state 예측)
    + reward_loss            # MSE (reward mean 예측)
    + 0.5 * reward_nll_loss  # Gaussian NLL: logvar 헤드 불확실성 학습
    - 0.001 * entropy        # 엔트로피 정규화 (다양성 장려)
    + 0.02 * prior_reg       # KL(policy ‖ empirical prior) (분포 편향 방지)
    + 0.3 * action_prior_loss  # ActionModel 지도 학습 (행동 prior 정확도)
)

# Gaussian NLL (logvar 헤드만 업데이트):
reward_nll_loss = 0.5 * (
    (reward_pred.detach() - rewards)² * exp(-reward_logvar) + reward_logvar
).mean()
```

### 6.3 학습 실행

```bash
# 자동 실행 (30일 달성 시 run_daily.py에서 자동 트리거)
python3 crawlers/run_daily.py

# 수동 전체 파이프라인 (학습 + 평가 + 릴리즈 게이트)
python3 backend/run_release_ready.py --steps 500 --batch-size 32 --lr 1e-4

# dry-run (학습 없이 평가 + 게이트만)
python3 backend/run_release_daily.py
```

### 6.4 품질 게이트 (릴리즈 판정 기준)

| 게이트 | 기준 | 의미 |
|--------|------|------|
| accuracy_raw | ≥ 0.55 | 방향성 정확도 55% 이상 |
| reward_raw | > 0.0 | 평균 보상 양수 |
| abstain_ratio | ≤ 0.85 | HOLD/SKIP 비율 85% 이하 |
| safe_override_ratio | ≤ 0.90 | 안전 게이트 발동 비율 90% 이하 |
| action_entropy_raw | ≥ 0.25 | 행동 다양성 |
| uplift_raw_vs_buy | ≥ 0.0 | always_buy 대비 개선 |
| no_mode_collapse_raw | 단일 행동 < 95% | 모드 붕괴 없음 |

---

## 7. 자동 학습 오케스트레이션

### 7.1 정책 (auto_training.py)

```
30일 데이터 미달 → release_check (드라이런)
30일 최초 달성  → train_release (첫 학습 + 릴리즈 게이트)
이후 7일 누적   → train_release (주기적 재학습)
7일 미만 누적   → release_check (데이터 대기)
```

### 7.2 환경 변수 설정

| 변수 | 기본값 | 설명 |
|------|--------|------|
| `GPU_ADVISOR_AUTO_TRAIN_ENABLED` | `true` | 자동 학습 활성화 |
| `GPU_ADVISOR_AUTO_TRAIN_TARGET_DAYS` | `30` | 첫 학습 기준 일수 |
| `GPU_ADVISOR_AUTO_RETRAIN_EVERY_DAYS` | `7` | 재학습 주기 |
| `GPU_ADVISOR_AUTO_TRAIN_TIMEOUT_SEC` | `5400` | 학습 타임아웃 (90분) |
| `GPU_ADVISOR_AUTO_TRAIN_STEPS` | `500` | 학습 스텝 수 |
| `GPU_ADVISOR_AUTO_TRAIN_BATCH_SIZE` | `32` | 배치 크기 |
| `GPU_ADVISOR_AUTO_TRAIN_LR` | `1e-4` | 학습률 |

### 7.3 상태 추적

```json
// data/processed/auto_training_state.json
{
  "updated_at": "2026-03-15T00:05:12",
  "last_action": "release_check",
  "last_reason": "insufficient_data_window",
  "last_trained_data_date": null,
  "last_pipeline_status": "not_ready"
}
```

### 7.4 CLI 옵션

```bash
# 자동 학습 비활성화 (드라이런만)
python3 crawlers/run_daily.py --disable-auto-train

# 재학습 주기 임시 변경 (오늘만 5일 기준)
python3 crawlers/run_daily.py --auto-retrain-days 5

# 릴리즈 파이프라인 완전 생략
python3 crawlers/run_daily.py --skip-release
```

---

## 8. 실전 사용법

### 8.1 서버 실행

```bash
python3 backend/simple_server.py
# http://localhost:8000/docs (Swagger UI)
```

### 8.2 API 사용

```bash
# GPU 구매 타이밍 추천
curl -X POST http://localhost:8000/api/ask \
  -H "Content-Type: application/json" \
  -d '{"model_name": "RTX 5060"}'
```

**응답 예시:**
```json
{
  "agent_trace": {
    "selected_action": "WAIT_SHORT",
    "raw_action": "WAIT_SHORT",
    "confidence": 0.421,
    "entropy": 0.92,
    "value": -0.123,
    "safe_mode": false,
    "action_probs": {
      "BUY_NOW": 0.12, "WAIT_SHORT": 0.42, "WAIT_LONG": 0.25,
      "HOLD": 0.14, "SKIP": 0.07
    },
    "expected_rewards": {
      "BUY_NOW": -0.008, "WAIT_SHORT": 0.012, "WAIT_LONG": 0.005,
      "HOLD": -0.001, "SKIP": -0.002
    }
  }
}
```

### 8.3 에이전트 평가 및 상태 확인

```bash
# 백테스트 평가
curl "http://localhost:8000/api/agent/evaluate?lookback_days=30"

# 데이터 준비도 확인
curl "http://localhost:8000/api/agent/readiness"

# 릴리즈 게이트 확인
curl "http://localhost:8000/api/agent/release-check"

# 시스템 상태
curl "http://localhost:8000/api/system/status"
```

---

## 9. 전체 데이터 흐름

### 9.1 일일 흐름 (LaunchAgent 00:00 자동)

```
00:00 - LaunchAgent 트리거
  ↓
[1/4] 다나와 GPU 가격 크롤링 (약 25초)
  ↓
[2/4] 환율 수집 (open.er-api.com)
  ↓
[3/4] GPU 뉴스 크롤링 (Google News RSS, 8개 키워드)
  ↓
[4/4] Feature Engineering (256차원 상태 벡터)
  ↓
상태 리포트 생성 (docs/reports/YYYY-MM-DD/data_status_*.json/md)
  ↓
자동 학습 오케스트레이션 (auto_training.py)
  ├─ 30일 미달 → release_check (드라이런 평가)
  └─ 30일 달성 → train_release (학습 + 게이트 + 리포트)
```

### 9.2 30일 데이터 윈도우 달성 시나리오

```
Day 1~29: 수집만 (release_check 드라이런)
Day 30:   자동 학습 트리거 (first_training_after_target)
  → AgentFineTuner: 500 steps, AdamW
  → AgentEvaluator: 30일 백테스트
  → 7개 게이트 판정
  → alphazero_model_agent_latest.pth 저장
  → 리포트 생성 (docs/reports/latest_release_report.json/md)

Day 37+:  7일 주기 재학습 (retrain_interval_reached)
```

---

## 10. 로드맵 및 일정

### Phase 1: 시스템 구축 ✅ (완료)

**기간:** 2026-02-14

- [x] AlphaZero 아키텍처 구현 (h/g/f + MCTS)
- [x] ActionModel (a) 추가 — 학습 가능한 행동 Prior
- [x] PUCT 공식 적용 (기존 UCB1 수정)
- [x] Dirichlet noise 활성화
- [x] 잔차 연결 추가 (h/g/f 전체)
- [x] Gaussian NLL reward loss 구현
- [x] 크롤러 시스템 (danawa + exchange + news + feature)
- [x] Feature Engineering (256차원)
- [x] LaunchAgent 자동화 (cron → LaunchAgent, TCC 문제 해결)
- [x] REST API 서버 (FastAPI)
- [x] 자동 학습 오케스트레이션 (30일 트리거, 7일 주기)
- [x] 릴리즈 파이프라인 (7개 품질 게이트)

### Phase 2: 데이터 수집 🔄 (진행 중)

**기간:** 2026-02-14 ~ 2026-03-15+ (30일 윈도우)

- [x] 매일 자정 자동 크롤링
- [ ] 30일 데이터 윈도우 충족 (현재 진행 중)

### Phase 3: 자동 학습 + 릴리즈 ⏳ (조건부 대기)

**트리거 조건:** `current_min_days >= 30`

- [ ] AgentFineTuner 자동 실행
- [ ] 7개 게이트 통과 (status="pass")
- [ ] `alphazero_model_agent_latest.pth` 배포

### Phase 4: 운영 최적화 🚀 (대기)

**기간:** Phase 3 완료 후

- [ ] 7일 주기 재학습 검증
- [ ] 백테스트 지표 모니터링 (accuracy, uplift, entropy)
- [ ] 하이퍼파라미터 튜닝 (필요 시)

---

## 11. FAQ

### Q1. 이 시스템은 진짜 AI인가요?

**A:** 예! AlphaGo와 동일한 원리의 AlphaZero 아키텍처에 추가 개선이 포함됩니다.

- ✅ 4개 신경망 (h/g/f/a, ~19M 파라미터)
- ✅ MCTS 탐색 (PUCT 공식, Dirichlet noise)
- ✅ 5개 손실 함수 기반 Fine-tuning
- ✅ 자동 학습 + 릴리즈 파이프라인

### Q2. 지금 당장 사용할 수 있나요?

**A:** 현재 체크포인트(`alphazero_model.pth`)가 있으면 즉시 사용 가능합니다.
30일 실데이터 학습 완료 후 `alphazero_model_agent_latest.pth`로 자동 업그레이드됩니다.

### Q3. 왜 30일이 필요한가요?

**A:** 시계열 Feature(MA7, MA14, MA30, MACD 등)와 가격 추이 패턴 학습에
최소 30일의 연속 데이터가 필요합니다.
특히 MACD는 26일 이상 이력이 있어야 실값이 계산됩니다.

### Q4. SKIP 행동은 언제 나오나요?

**A:** SKIP은 가격 변화율 기반 레이블링 대상이 아닙니다.
정책 보정 단계에서 MCTS / ActionModel의 탐색 결과로 선택될 수 있으며,
안전 게이트(신뢰도 < 0.25 또는 엔트로피 > 1.58)가 발동하면 HOLD로 대체됩니다.

### Q5. Mac이 아니면 안 되나요?

**A:** 아니요. LaunchAgent는 macOS 전용이지만, 크론(cron)이나 systemd로 대체 가능합니다.

```bash
# Linux/Windows: 직접 cron 또는 Task Scheduler로 run_daily.py 실행
python3 crawlers/run_daily.py
```

MPS 가속은 Apple Silicon 전용이지만, CUDA/CPU로 자동 폴백됩니다.

---

## 부록 A: 파일 구조

```
gpu-advisor/
├── crawlers/                              # 크롤러 + 자동화
│   ├── danawa_crawler.py                  # 다나와 GPU 가격
│   ├── exchange_rate_crawler.py           # 환율
│   ├── news_crawler.py                    # 뉴스 + 감성 분석
│   ├── feature_engineer.py                # 256차원 Feature
│   ├── auto_training.py                   # 자동 학습 오케스트레이션
│   ├── status_report.py                   # 일일 상태 리포트
│   └── run_daily.py                       # 일일 실행 (락 기반)
│
├── backend/                               # AI 엔진 + API
│   ├── models/
│   │   ├── representation_network.py      # h(s): 잔차 FeedForward
│   │   ├── dynamics_network.py            # g(s,a): World Model + Gaussian reward
│   │   ├── prediction_network.py          # f(s): Policy + Value
│   │   ├── action_model.py                # a(z): ActionEmbedding + ActionPrior
│   │   └── mcts_engine.py                 # PUCT + Dirichlet noise
│   ├── agent/
│   │   ├── gpu_purchase_agent.py          # 메인 에이전트 (4-signal calibration)
│   │   ├── fine_tuner.py                  # Fine-tuning loop (5 losses)
│   │   ├── evaluator.py                   # 백테스트 평가기
│   │   ├── release_pipeline.py            # 7개 품질 게이트
│   │   └── next_steps.py                  # 이중 언어 다음 단계 안내
│   ├── simple_server.py                   # FastAPI 서버 (SSE, auth, SQLite)
│   ├── run_release_ready.py               # 전체 파이프라인 (학습+평가+릴리즈)
│   └── run_release_daily.py               # 드라이런 (평가+릴리즈 게이트만)
│
├── data/                                  # 데이터 저장소
│   ├── raw/danawa/YYYY-MM-DD.json
│   ├── raw/exchange/YYYY-MM-DD.json
│   ├── raw/news/YYYY-MM-DD.json
│   └── processed/dataset/training_data_YYYY-MM-DD.json
│
├── docs/reports/                          # 자동 생성 리포트
│   ├── YYYY-MM-DD/
│   │   ├── data_status_*.json/md
│   │   ├── release_report_*.json/md
│   │   └── auto_training_status_*.json/md
│   ├── latest_data_status.json/md
│   ├── latest_release_report.json/md
│   └── latest_auto_training_status.json/md
│
├── alphazero_model.pth                    # 초기 체크포인트 (합성 데이터)
├── alphazero_model_agent_latest.pth       # 실데이터 학습 후 체크포인트
└── logs/daily_crawl.log                   # 상세 Python 로그
```

---

## 부록 B: 명령어 치트시트

### 크롤러

```bash
# 즉시 실행
python3 crawlers/run_daily.py

# 릴리즈 파이프라인 생략 (수집만)
python3 crawlers/run_daily.py --skip-release

# 로그 확인
tail -f ~/Library/Logs/gpu-advisor/cron.log  # LaunchAgent 로그
tail -f logs/daily_crawl.log                 # Python 상세 로그
```

### 서버

```bash
# 백엔드 시작
python3 backend/simple_server.py

# API 테스트
curl -X POST http://localhost:8000/api/ask \
  -H "Content-Type: application/json" \
  -d '{"model_name": "RTX 5060"}'
```

### 학습 + 릴리즈

```bash
# 전체 파이프라인 (학습 + 게이트)
python3 backend/run_release_ready.py

# 게이트 드라이런 (학습 없이)
python3 backend/run_release_daily.py

# 데이터 준비도 확인
curl http://localhost:8000/api/agent/readiness

# 백테스트 평가
curl "http://localhost:8000/api/agent/evaluate?lookback_days=30"
```

---

## 부록 C: 참고 자료

### 논문

1. **AlphaGo** (2016) — Silver et al., Nature 529
2. **AlphaZero** (2017) — Silver et al., arXiv:1712.01815
3. **MuZero** (2020) — Schrittwieser et al., Nature 588

### 내부 문서

- `docs/MCTS_WALKTHROUGH_KR.md` — PUCT 수치 예제 워크스루
- `docs/INFERENCE_WALKTHROUGH_KR.md` — 추론 전체 흐름 추적
- `docs/WORLD_MODEL_THEORY_AND_IMPLEMENTATION.md` — h/g/f/a/MCTS 코드 매핑
- `docs/AI_COMPONENTS_AND_IMPLEMENTATION_KR.md` — 6개 축 AI 구성요소 해설
- `docs/GLOSSARY_KR.md` — 용어 정리 (PUCT, 잔차 연결, ActionModel 등)

---

## 맺음말

이 프로젝트는 **AlphaZero를 실생활 구매 결정 문제에 적용한 사례**입니다.

### 핵심 성과

✅ **완전한 AlphaZero 구현** — h/g/f + ActionModel a + MCTS (PUCT, Dirichlet noise)
✅ **자동화된 데이터 파이프라인** — 크롤러 4종 + LaunchAgent (macOS TCC 해결)
✅ **자동 학습 오케스트레이션** — 30일 트리거 + 7일 재학습 주기
✅ **운영급 릴리즈 파이프라인** — 7개 품질 게이트 + 이중 언어 리포트

### 현재 상태 (2026-03-15)

- 아키텍처: 100% 완성
- 데이터 수집: 일일 정상 동작
- 30일 윈도우: 달성 임박 → 자동 학습 대기 중
- 다음 단계: 30일 달성 → 자동 학습 → 릴리즈 게이트 통과 확인

---

**문서 버전:** 2.0
**최종 수정:** 2026-03-15
**개발자:** 진영화 (Younghwa Jin) timotolkie@gmail.com
